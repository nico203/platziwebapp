import { FiltrarTareasPipe } from './filtrar-tareas.pipe';

describe('FiltrarTareasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarTareasPipe();
    expect(pipe).toBeTruthy();
  });
});
