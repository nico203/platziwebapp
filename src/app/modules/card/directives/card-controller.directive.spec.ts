import { CardControllerDirective } from './card-controller.directive';

describe('CardControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new CardControllerDirective(null);
    expect(directive).toBeTruthy();
  });
});
